---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: gitlab-staging-minio
  namespace: gitlab
spec:
  dnsNames:
  - minio.gitlab.staging.spack.io
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt
  secretName: tls-gitlab-staging-minio
