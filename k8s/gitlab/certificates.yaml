---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: gitlab-webservice
  namespace: gitlab
spec:
  secretName: tls-gitlab-webservice
  issuerRef:
    name: letsencrypt-staging
    kind: ClusterIssuer
  dnsNames:
    - gitlab.test.spack.io

---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: gitlab-registry
  namespace: gitlab
spec:
  secretName: tls-gitlab-registry
  issuerRef:
    name: letsencrypt-staging
    kind: ClusterIssuer
  dnsNames:
    - registry.gitlab.test.spack.io

---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: gitlab-minio
  namespace: gitlab
spec:
  secretName: tls-gitlab-minio
  issuerRef:
    name: letsencrypt-staging
    kind: ClusterIssuer
  dnsNames:
    - minio.gitlab.test.spack.io
