[project]
name = "gh-gl-sync"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "PyGithub~=2.9.1",
    "python-dateutil~=2.8.2",
    "requests~=2.32.5",
    "sentry-sdk~=1.32.0",
]

[dependency-groups]
dev = [
    "flake8~=4.0.1",
    "pytest~=6.2.5",
    "pytest-cov~=3.0.0",
]
