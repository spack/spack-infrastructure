export GITLAB_WEB_PORT=8080
export GITLAB_INTERNAL_WEB_PORT=10080
export CDASH_WEB_PORT=8081
export CDASH_INTERNAL_WEB_PORT=80

export UPSTREAM_REPO="git://github.com/opadron/spack"
export UPSTREAM_REF="ci-demo"

export GITLAB_ROOT_PASSWORD=REQUIRED
export CDASH_ROOT_PASSWORD=REQUIRED

export RUNNER_REGISTRATION_TOKEN=SET-AFTER-DEPLOYING-GITLAB

export DOWNSTREAM_CI_REPO=\
"http://root:${GITLAB_ROOT_PASSWORD}@gitlab:${GITLAB_INTERNAL_WEB_PORT}"\
"/root/spack-ci"
