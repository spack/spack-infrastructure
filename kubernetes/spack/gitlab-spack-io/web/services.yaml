---
apiVersion: v1
kind: Service
metadata:
  name: gitlab
  namespace: spack
  labels:
    app: gitlab
    srv: web
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: http
  selector:
    app: gitlab
    srv: web
